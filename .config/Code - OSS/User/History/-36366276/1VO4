#[[ -z $DISPLAY && $(tty) = /dev/tty1 ]] && exec ~/.startplasma
[[ -z $DISPLAY && $(tty) = /dev/tty1 ]] && exec ~/.startplasma
[[ -z $DISPLAY && $(tty) = /dev/tty2 ]] && exec hyprland

export GTK_THEME=Adwaita-dark

